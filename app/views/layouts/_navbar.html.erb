
<nav class="navbar navbar-expand-lg navbar-light bg-dark">
  <div class="container-fluid">
  
    <div class="bg-dark">
      <h1 class="text-white">JRTransit</h1>
    </div>
      
  <% if current_user%>
  <div>
    <form class="form-inline">
    <% if current_user.is_a?(Admin)%>
    <%= link_to "Show admins panel", admin_transits_path, class: "btn btn-sm btn-outline-secondary", type: "button" %>
    <%end%>
      <%= link_to "Show current user transits", user_transits_path(current_user), class: "btn btn-sm btn-outline-secondary", type: "button" %>
      <%= link_to "#{current_user.email} logout", destroy_user_session_path, method: :delete, class: "btn btn-sm btn-outline-secondary", type: "button" %>
    </form>
  </div>
  
  <%else%>
  <div>
    <form class="form-inline">
      <%= link_to "user login", new_user_session_path, class: "btn btn-sm btn-outline-secondary", type: "button" %>
      <%= link_to "Create new user", new_user_registration_path, class: "btn btn-sm btn-outline-secondary", type: "button" %>
    </form>
    </div>
  <%end%>
  

  </div>
</nav>